<Page x:Class="Pr17Danilov.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Товары - Космическая ложа">

    <Page.Resources>
        <local:DiscountToBrushConverter x:Key="DiscountToBrushConverter"/>
        <local:BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </Page.Resources>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#FF6B6B" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Корзина 🛒" Command="{Binding GoToCartCommand}" 
                        Margin="5" Width="100" Height="35" Background="White" Cursor="Hand"/>
                <Button Content="← Назад" Command="{Binding GoBackCommand}" 
                        Margin="5" Width="80" Height="35" Background="White" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Фильтры -->
        <Border Grid.Row="1" Background="White" Margin="10" Padding="15" CornerRadius="10">
            <StackPanel>
                <TextBlock Text="Поиск и фильтры" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="5" Height="35" ToolTip="Поиск по названию"/>

                    <ComboBox Grid.Column="1" ItemsSource="{Binding Manufacturers}" 
                              DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedManufacturer}"
                              Margin="5" Height="35"/>

                    <ComboBox Grid.Column="2" ItemsSource="{Binding ProductTypes}" 
                              DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedProductType}"
                              Margin="5" Height="35"/>

                    <ComboBox Grid.Column="3" SelectedItem="{Binding SelectedSort}" Margin="5" Height="35">
                        <ComboBoxItem Content="Без сортировки"/>
                        <ComboBoxItem Content="По возрастанию"/>
                        <ComboBoxItem Content="По убыванию"/>
                    </ComboBox>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Товары сеткой -->
        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding FilteredProducts}" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding Discount, Converter={StaticResource DiscountToBrushConverter}}"
                                Width="250" Height="350" Margin="10" CornerRadius="10" 
                                BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel Margin="10">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" 
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" 
                                           MaxHeight="80" Margin="0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,10">
                                    <TextBlock Text="Цена: " FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Price, StringFormat={}{0:C}}"/>
                                    <TextBlock Text="{Binding Discount, StringFormat=' (скидка {0}%)'}" 
                                               Foreground="Red" Visibility="{Binding Discount, Converter={StaticResource DiscountVisibilityConverter}}"/>
                                </StackPanel>

                                <TextBlock Text="⭐ {Binding Rating, StringFormat={}{0:F1}}" Margin="0,5"/>

                                <StackPanel Orientation="Horizontal" Margin="0,10">
                                    <Button Content="Подробнее" Command="{Binding DataContext.ShowProductDetailsCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=Page}}" 
                                            CommandParameter="{Binding}" Width="100" Margin="0,0,5,0"/>
                                    <Button Content="В корзину" Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource FindAncestor, AncestorType=Page}}" 
                                            CommandParameter="{Binding}" Background="#4CAF50" Foreground="White" Width="100"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>